const { admin, deployProxy } = require('@openzeppelin/truffle-upgrades')

const SoapPunkAlchemy = artifacts.require('SoapPunkAlchemy')

module.exports = async function (deployer, network, accounts) {
}
